Readiness contributor row: name left, colored state word right, thin filled track below. Stack 3–4 with 10px gap next to a ScoreRing.

```jsx
<ContributorBar name="Resting HR" state="optimal" />
<ContributorBar name="Recovery index" state="pay attention" />
```
