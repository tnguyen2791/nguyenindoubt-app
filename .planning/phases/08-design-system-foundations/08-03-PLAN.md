---
phase: 08-design-system-foundations
plan: 03
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - lib/screens/journal_screen.dart
  - lib/screens/resources_screen.dart
  - lib/screens/app_shell.dart
autonomous: true
requirements: [DS-01, DS-02, DS-03, DS-04, DS-05]
must_haves:
  truths:
    - "Journal, resources, and safety pills pass a semantic PillTone (clinician hidden->private, entry mood->neutral, crisis category->flag, non-crisis category->neutral); no StatusPill passes a raw color override (DS-03)."
    - "The single BrandMark widget replaces the three raw logo treatments in app_shell — the 34px AppBar tile, the 82px onboarding hero mark, and the 64px patient-onboarding mark (DS-05)."
    - "No text in these three screens uses a weight heavier than w700; emphasis captions/labels are w600 max (DS-01)."
    - "Freehand paddings/gaps and list content paddings reference NidSpace/NidRadius (DS-02)."
    - "The reset-demo and safety dialogs render on the blessed dialogTheme (no fromSeed tint), and no stray stock color remains (DS-04)."
  artifacts:
    - lib/screens/journal_screen.dart
    - lib/screens/resources_screen.dart
    - lib/screens/app_shell.dart
  key_links:
    - "app_shell.dart gains an import of common_widgets.dart to use BrandMark; journal/resources already import it."
    - "resources_screen migrates off the StatusPill color override to PillTone, retiring the last color: call-site."
---

<objective>
Finish the Wave-2 call-site sweep on the remaining surfaces: the journal, the resources/safety screen, and the app shell (AppBar + onboarding). Migrate pills to PillTone, replace the three raw logo treatments with the single BrandMark, de-weight emphasis labels, and tokenize spacings — verbatim copy, no behavior change.

Purpose: app_shell is where the "three-radii/two-treatment" logo problem and the raw 34px muddy AppBar tile live; resources holds the last StatusPill `color:` override. Closing these completes DS-03 and DS-05 across the app.
Output: Tone-driven, BrandMark-unified, tokenized `journal_screen.dart`, `resources_screen.dart`, and `app_shell.dart`.

Implements DS-03 (pill tones), DS-05 (single BrandMark), DS-01 (de-weight), DS-02 (token sweep), DS-04 (themed dialogs / no stray color) at these call-sites.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-design-system-foundations/08-CONTEXT.md
@.planning/phases/08-design-system-foundations/08-01-SUMMARY.md
@lib/screens/journal_screen.dart
@lib/screens/resources_screen.dart
@lib/screens/app_shell.dart
@lib/screens/common_widgets.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep journal_screen.dart — pill tones, de-weight, tokens</name>
  <files>lib/screens/journal_screen.dart</files>
  <read_first>lib/screens/journal_screen.dart (BrandHeader 'clinician hidden' pill, entry moodTag StatusPill, the 'private by default' labelSmall w700 caption, gaps 16/12/10/8, ListView pad 20), lib/screens/common_widgets.dart (PillTone from 08-01), lib/theme/tokens.dart</read_first>
  <action>Import `../theme/tokens.dart`. Mechanical sweep only; keep every label string and journal copy verbatim.

DS-03 — the BrandHeader trailing 'clinician hidden' pill uses `PillTone.private`; the per-entry `StatusPill(label: entry.moodTag ?? 'private')` uses `PillTone.neutral` (mood is descriptive, not a safety state). Do not pass `color:`.

DS-01 — the 'private by default' caption currently uses labelSmall with a w700 emphasis; reduce it to w600 max (use the theme `labelSmall` which is already w600, keeping the canopy color via copyWith). No weight above w700 anywhere.

DS-02 — replace freehand spacings: ListView `EdgeInsets.all(20)` -> `EdgeInsets.all(NidSpace.xl)`; the `SizedBox(height: 16/12/10/8)` rhythm and the per-entry `EdgeInsets.only(bottom: 12)` -> nearest NidSpace. Do not change the TextFields, SegmentedButton, save-entry behavior, or any entry copy.</action>
  <verify>
    <automated>grep -q "PillTone.private" lib/screens/journal_screen.dart && grep -q "PillTone.neutral" lib/screens/journal_screen.dart && grep -q "NidSpace" lib/screens/journal_screen.dart && flutter analyze lib/screens/journal_screen.dart</automated>
  </verify>
  <acceptance_criteria>
    - 'clinician hidden' pill is private tone; entry mood pill is neutral tone; no `color:` override.
    - The 'private by default' caption uses no weight heavier than w700.
    - List and gap spacings reference NidSpace; `flutter analyze` clean for the file.
  </acceptance_criteria>
  <done>Journal pills encode privacy state, emphasis caption is de-weighted, spacings tokenized (DS-03, DS-01, DS-02).</done>
</task>

<task type="auto">
  <name>Task 2: Sweep resources_screen.dart — retire the color override, pill tones, de-weight, tokens</name>
  <files>lib/screens/resources_screen.dart</files>
  <read_first>lib/screens/resources_screen.dart (_ResourceCard category StatusPill with the color: override, disclaimer w800 label, SafetyScreen '988 / 911' pill + _showSafetyInstructions AlertDialog, ListView/GridView pad 20, gaps 12/10/8), lib/screens/common_widgets.dart (PillTone from 08-01), lib/theme/tokens.dart</read_first>
  <action>Import `../theme/tokens.dart`. Keep all resource/safety/disclaimer strings verbatim (safety tests assert 'Call or text 988 now.' and 'call 911').

DS-03 — replace the `_ResourceCard` category pill's `color:` override with `tone: card.crisisFlag ? PillTone.flag : PillTone.neutral` (this retires the last StatusPill color override in the app). The SafetyScreen '988 / 911' pill uses `PillTone.neutral` (keep it calm).

DS-01 — the disclaimer text currently uses a labelSmall with a heavy weight; reduce it to w600 max via the theme `labelSmall` (keep the crisis/canopy color via copyWith). No weight above w700.

DS-02 — replace freehand spacings: the ListView.separated and GridView.count `EdgeInsets.all(20)` -> `NidSpace.xl`; `crossAxisSpacing/mainAxisSpacing: 12` and `SizedBox(height/width: 12/10/8)` -> nearest NidSpace; keep the column count breakpoints and childAspectRatio.

DS-04 — no code change is required for the safety `AlertDialog` (it now inherits the blessed dialogTheme from 08-01); just confirm no stray stock color is introduced. Do not change the dialog copy or the crisis-icon logic.</action>
  <verify>
    <automated>grep -q "PillTone.flag" lib/screens/resources_screen.dart && grep -q "PillTone.neutral" lib/screens/resources_screen.dart && grep -q "NidSpace" lib/screens/resources_screen.dart && ! grep -q "FontWeight.w8" lib/screens/resources_screen.dart && flutter analyze lib/screens/resources_screen.dart</automated>
  </verify>
  <acceptance_criteria>
    - The category pill uses flag/neutral tone with no `color:` override; SafetyScreen pill is neutral.
    - Disclaimer uses no weight heavier than w700.
    - List/grid/gap spacings reference NidSpace; `flutter analyze` clean for the file.
    - Safety dialog copy is unchanged.
  </acceptance_criteria>
  <done>Resources/safety pills encode state, the last color override is retired, disclaimer de-weighted, spacings tokenized (DS-03, DS-01, DS-02, DS-04).</done>
</task>

<task type="auto">
  <name>Task 3: Sweep app_shell.dart — one BrandMark, de-weight, tokens</name>
  <files>lib/screens/app_shell.dart</files>
  <read_first>lib/screens/app_shell.dart (_AppBar raw 34px ClipRRect radius 6 mark; _HeroCopy 82px raw Image + bodyMedium w700 'Demo auth is local' copy; _PatientOnboardingScreen 64px raw Image; _DemoNotice bodySmall w700 disclosure copy; reset AlertDialog; paddings 24/28/22/18/14/10), lib/screens/common_widgets.dart (BrandMark from 08-01), lib/theme/tokens.dart</read_first>
  <action>Add `import 'common_widgets.dart';` and `import '../theme/tokens.dart';`. Keep every string literal verbatim (tests assert the demo-notice, onboarding disclosure, and reset-dialog copy exactly).

DS-05 — replace all three raw logo treatments with the single `BrandMark`: in `_AppBar`, replace the `ClipRRect(borderRadius: 6) + Image.asset(... 34x34)` with `BrandMark(size: 34)` (or the min-size from 08-01), keeping the adjacent 'NguyenInDoubt' text; in `_HeroCopy`, replace the raw 82px `Image.asset` with `BrandMark(size: 82)`; in `_PatientOnboardingScreen`, replace the raw 64px `Image.asset` with `BrandMark(size: 64)`. This kills the muddy raw AppBar tile and the three-radii/two-treatment usage.

DS-01 — soften emphasis weights to w600 max: `_DemoNotice`'s disclosure `bodySmall` w700 -> w600 (keep the canopy color and the exact text); `_HeroCopy`'s 'Demo auth is local to this device...' `bodyMedium` w700 -> w600. Headings (`displaySmall`, `headlineMedium`) stay w700. No weight above w700.

DS-02 — sweep freehand paddings/gaps to NidSpace: the onboarding `EdgeInsets.all(24)`, `_DemoNotice` `symmetric(horizontal: 16, vertical: 10)`, hero/panel gaps (28/22/18/14/12/10/8), and the `_StagPanel` `EdgeInsets.all(18)` -> nearest NidSpace; logo/panel radii (8/14) -> NidRadius.card/badge. Do not change the NavigationBar/NavigationRail destinations, the reset-demo flow, sign-out, or any copy.</action>
  <verify>
    <automated>grep -q "BrandMark" lib/screens/app_shell.dart && ! grep -q "FontWeight.w8" lib/screens/app_shell.dart && ! grep -q "FontWeight.w9" lib/screens/app_shell.dart && grep -q "NidSpace" lib/screens/app_shell.dart && flutter analyze && flutter test</automated>
  </verify>
  <acceptance_criteria>
    - All three raw logo Image.asset treatments are replaced by BrandMark; no raw 34px ClipRRect logo tile remains in the AppBar.
    - No weight heavier than w700 in the file; disclosure/hero emphasis is w600 max with copy unchanged.
    - Paddings/gaps/radii reference NidSpace/NidRadius.
    - `flutter analyze` clean and `flutter test` (27) all pass (demo-notice, onboarding, reset-dialog copy intact).
  </acceptance_criteria>
  <done>The app shell uses one BrandMark everywhere the logo appears, emphasis is de-weighted, spacings tokenized, and all 27 tests stay green (DS-05, DS-01, DS-02).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | Presentation-only sweep of three existing screens — no new input, network, data path, or dependency. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-05 | Tampering | local-only demo disclosure copy | low | mitigate | Sweep is BrandMark/tone/spacing/weight only; the 'Demo mode: data stays on this device...', onboarding, and reset-dialog strings are left verbatim and asserted by the existing tests. |
| T-08-06 | Spoofing | BrandMark logo lockup | low | accept | BrandMark renders the same bundled brand asset; a single enforced lockup reduces inconsistent/forged-looking logo treatments rather than adding risk. |
| T-08-SC | Tampering | pub package installs | low | accept | No pubspec.yaml change — no new packages this plan. |
</threat_model>

<verification>
- `dart format lib test` leaves no diff.
- `flutter analyze` reports "No issues found!".
- `flutter test` — all 27 tests pass.
- `flutter build web` succeeds (final integration — BrandMark in the AppBar/onboarding + all theme changes must survive a release web build for the GitHub Pages deploy).
- app_shell references BrandMark and NidSpace; no `color:` override in resources; no weight above w700 in any of the three files.
- Phase-completion (Wave 2 done): `grep -rEn 'FontWeight\.w(800|900)' lib/ | grep -v '//'` returns nothing across the whole `lib/` tree.
</verification>

<success_criteria>
Journal, resources/safety, and app shell render pills by semantic tone, use the single BrandMark for every logo appearance, contain no weight above w700 and no stray stock color, and use NidSpace/NidRadius — with all 27 tests green and analyze clean.
</success_criteria>

<output>
Create `.planning/phases/08-design-system-foundations/08-03-SUMMARY.md` when done. Note the BrandMark sizes used per surface and the pill tones chosen, and confirm the last StatusPill color override is retired.
</output>
