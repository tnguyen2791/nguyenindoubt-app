---
phase: 08-design-system-foundations
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - lib/screens/patient_dashboard.dart
  - lib/screens/clinician_dashboard.dart
autonomous: true
requirements: [DS-01, DS-02, DS-03, DS-04]
must_haves:
  truths:
    - "Every StatusPill on the patient and clinician dashboards passes a semantic PillTone (shared->good, private->private, sharing active->good, sharing revoked->flag, invite required->neutral, link statuses mapped by state) instead of the default mint chip (DS-03)."
    - "The stray Colors.red.shade700 in the invite-validation message is replaced with NidColors.ember (DS-04)."
    - "The _MetricTile and _ClinicianMetric caps labels no longer use a weight heavier than w700; they read the theme label style (DS-01)."
    - "Freehand paddings/gaps and the ListView content padding on both dashboards reference NidSpace/NidRadius (DS-02)."
  artifacts:
    - lib/screens/patient_dashboard.dart
    - lib/screens/clinician_dashboard.dart
  key_links:
    - "Both screens import PillTone/StatusPill from common_widgets.dart and NidSpace/NidRadius from tokens.dart established in 08-01."
    - "Pill tone is the connection point where dashboard color now encodes sharing/permission/link state."
---

<objective>
Sweep the patient and clinician dashboards onto the Wave-1 foundations: pass semantic PillTone to every StatusPill, replace the stray stock red, drop the heavy caps-label weights, and move freehand spacings onto the NidSpace/NidRadius grid. Mechanical mapping only — no copy, layout, or behavior changes.

Purpose: These two dashboards carry the most status pills and metric tiles, so they are where "green-confetti" default pills and w900 caps labels are most visible. Fixing them proves the tone/weight system end-to-end.
Output: Tone-driven, tokenized `patient_dashboard.dart` and `clinician_dashboard.dart`.

Implements DS-03 (pill tones), DS-04 (Colors.red -> NidColors.ember), DS-01 (de-weight caps labels), DS-02 (token sweep) at these call-sites.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-design-system-foundations/08-CONTEXT.md
@.planning/phases/08-design-system-foundations/08-01-SUMMARY.md
@lib/screens/patient_dashboard.dart
@lib/screens/clinician_dashboard.dart
@lib/screens/common_widgets.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep patient_dashboard.dart — pill tones, stock-red fix, de-weight, tokens</name>
  <files>lib/screens/patient_dashboard.dart</files>
  <read_first>lib/screens/patient_dashboard.dart (all StatusPill call-sites, the _healthPermission* helpers, _InviteValidationMessage's Colors.red.shade700, _MetricTile w900 caps label, ListView pad 20 and gap SizedBoxes), lib/screens/common_widgets.dart (PillTone values + StatusPill tone API from 08-01), lib/theme/tokens.dart</read_first>
  <action>Import `../theme/tokens.dart`. Apply the tone/weight/token sweep per DS-03/DS-04/DS-01/DS-02 without changing any label string, copy, or behavior.

DS-03 pill tones — pass `tone:` to each StatusPill: the BrandHeader trailing pill uses `PillTone.good` when consent is granted ('shared sleep') and `PillTone.private` otherwise ('private'). Add a small helper `PillTone _healthPermissionTone(HealthPermissionStatus)` mapping ready->good, denied->flag, revoked->flag, unavailable->caution, notRequested->neutral, partial->caution, and pass it to the health-permission StatusPill. In `_ConsentLifecycleCard`, the status pill uses good for 'sharing active', flag for 'sharing revoked', neutral for 'invite required'; the clinicCode pill uses `PillTone.neutral`. Do not pass `color:` anywhere.

DS-04 — in `_InviteValidationMessage`, replace the stray stock red (`Colors.red.shade700`) with `NidColors.ember` (the border/foreground tone for the not-available case). Do not leave the literal in a comment.
<!-- planner-discipline-allow: Colors.red -->

DS-01 — in `_MetricTile`, remove the heavy caps-label weight (the w900) and render the uppercased label with the theme's `labelSmall` (w600) via `Theme.of(context).textTheme.labelSmall` (optionally copyWith color NidColors.canopy); keep the `.toUpperCase()` and the value/caption styles.

DS-02 — replace freehand paddings/gaps with NidSpace: the ListView `EdgeInsets.all(20)` -> `EdgeInsets.all(NidSpace.xl)`; the `SizedBox(height: 16/12/8/4)` rhythm and `Wrap` spacings (12/10/8) -> nearest NidSpace (l/m/s/xs); the `_MetricTile` width may stay fixed. Do not alter the Import button, permission-message text, consent flow, or SleepTrendBars usage.</action>
  <verify>
    <automated>! grep -q "Colors.red" lib/screens/patient_dashboard.dart && ! grep -q "FontWeight.w9" lib/screens/patient_dashboard.dart && grep -q "PillTone" lib/screens/patient_dashboard.dart && grep -q "NidSpace" lib/screens/patient_dashboard.dart && flutter analyze lib/screens/patient_dashboard.dart</automated>
  </verify>
  <acceptance_criteria>
    - Every StatusPill passes a PillTone; no `color:` override and no default-mint pill remains.
    - `Colors.red.shade700` is gone, replaced by `NidColors.ember`.
    - `_MetricTile` caps label uses no weight heavier than w700.
    - ListView/gap spacings reference NidSpace; `flutter analyze` clean for the file.
  </acceptance_criteria>
  <done>Patient dashboard pills encode state, the stock red is gone, caps labels are de-weighted, and spacings are tokenized (DS-03, DS-04, DS-01, DS-02).</done>
</task>

<task type="auto">
  <name>Task 2: Sweep clinician_dashboard.dart — pill tones, de-weight, tokens</name>
  <files>lib/screens/clinician_dashboard.dart</files>
  <read_first>lib/screens/clinician_dashboard.dart (BrandHeader trailing 'no messaging' pill, _PatientList link-status StatusPill + _linkStatusLabel, _PatientDetail 'journal private' pill, _ClinicianMetric w900 caps + mint tile radius 8/pad 14, gaps), lib/screens/common_widgets.dart (PillTone from 08-01), lib/theme/tokens.dart</read_first>
  <action>Import `../theme/tokens.dart`. Apply the same mechanical sweep; keep all label strings and the sleep-only privacy copy verbatim (tests assert on them).

DS-03 pill tones — the BrandHeader trailing 'no messaging' pill uses `PillTone.neutral`. Add a helper `PillTone _linkStatusTone(LinkStatus)` mapping accepted->good, pending->caution, revoked->flag, expired->caution, and pass it to the non-actionable link StatusPill in `_PatientList`. The `_PatientDetail` 'journal private' pill uses `PillTone.private`.

DS-01 — in `_ClinicianMetric`, remove the heavy caps-label weight (the w900) and use the theme `labelSmall` (w600) for the uppercased label; keep the value style (headlineSmall) and `.toUpperCase()`.

DS-02 — replace freehand values with tokens: the mint metric tile `BorderRadius.circular(8)` -> `NidRadius.card`, its `EdgeInsets.all(14)` -> `EdgeInsets.all(NidSpace.m)` (or l); ListView `EdgeInsets.all(20)` -> `NidSpace.xl`; the `SizedBox(height: 16/14/10/8/2)` and `Wrap`/`Row` spacings -> nearest NidSpace; the fixed `SizedBox(width: 310/140)` may stay. Do not change the CircleAvatar colors, the InkWell tap behavior, `_linkStatusLabel`, or the 'Visible: ... Hidden: ...' visibility copy.</action>
  <verify>
    <automated>! grep -q "FontWeight.w9" lib/screens/clinician_dashboard.dart && grep -q "PillTone" lib/screens/clinician_dashboard.dart && grep -q "NidRadius" lib/screens/clinician_dashboard.dart && grep -q "NidSpace" lib/screens/clinician_dashboard.dart && flutter analyze lib/screens/clinician_dashboard.dart && flutter test</automated>
  </verify>
  <acceptance_criteria>
    - 'no messaging' pill is neutral; link-status pills map accepted/pending/revoked/expired to good/caution/flag/caution; 'journal private' pill is private.
    - `_ClinicianMetric` caps label uses no weight heavier than w700.
    - Metric-tile radius/padding and list spacings reference NidRadius/NidSpace.
    - `flutter analyze` clean and `flutter test` (27) all pass (sleep-only privacy strings intact).
  </acceptance_criteria>
  <done>Clinician dashboard pills encode link/messaging state, caps labels are de-weighted, spacings/radii are tokenized, and all 27 tests stay green (DS-03, DS-01, DS-02).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | Presentation-only sweep of two existing screens — no new input, network, data path, or dependency. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-03 | Information disclosure | clinician sleep-only privacy copy | low | mitigate | Sweep is tone/spacing-only; the 'Visible/Hidden' and 'Sleep summaries, never journals' strings are left verbatim and asserted by the existing clinician tests. |
| T-08-04 | Tampering | consent/permission pill labels | low | accept | Pill labels ('sharing active/revoked', 'invite required', 'sleep access ready') are unchanged — only their PillTone is set; consent tests still pass. |
| T-08-SC | Tampering | pub package installs | low | accept | No pubspec.yaml change — no new packages this plan. |
</threat_model>

<verification>
- `dart format lib test` leaves no diff.
- `flutter analyze` reports "No issues found!".
- `flutter test` — all 27 tests pass.
- No `Colors.red` and no weight above w700 in either dashboard; both reference PillTone and NidSpace/NidRadius.
</verification>

<success_criteria>
Both dashboards render every pill by semantic tone, contain no stock Material color and no weight above w700, and use NidSpace/NidRadius for spacing/radius — with all 27 tests green and analyze clean.
</success_criteria>

<output>
Create `.planning/phases/08-design-system-foundations/08-02-SUMMARY.md` when done. Note the PillTone chosen for each call-site and any spacing roundings applied.
</output>
