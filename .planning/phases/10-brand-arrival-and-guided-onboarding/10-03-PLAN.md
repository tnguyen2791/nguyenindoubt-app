---
phase: 10-brand-arrival-and-guided-onboarding
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - lib/screens/patient_first_run.dart
  - lib/screens/patient_dashboard.dart
  - test/widget_test.dart
autonomous: true
requirements: [ONB-04]
user_setup: []

must_haves:
  truths:
    - "When state.summaries.isEmpty the dashboard shows ONE calm guided hero with a single primary 'Import sleep' action instead of three '--' metric tiles and the empty trend chart (per D ONB-04)."
    - "The guided hero states the sleep-only promise ('never your journal'), doubling as sleep-only permission priming shown before the OS prompt (per D ONB-04)."
    - "The empty '--' metric tiles and the empty sleep-trend section are demoted/hidden until there is data; once sleep is imported the normal metrics + trend appear (per D ONB-04)."
    - "The first-run layer is a modular widget so Phase 11 (dashboard hierarchy + insights) extends rather than fights it (per D ONB-04)."
  artifacts:
    - "lib/screens/patient_first_run.dart — modular PatientFirstRun widget (guided hero + sleep-only priming + Import sleep action)"
    - "patient_dashboard.dart empty-branch wiring (summaries.isEmpty -> first-run; else metrics + trend)"
    - "Updated widget_test.dart empty-state assertions + post-import flow"
  key_links:
    - "PatientFirstRun 'Import sleep' button -> state.importMockSleep (disabled while busy or health unavailable)"
    - "state.summaries.isEmpty gate selects first-run vs the data dashboard"
---

<objective>
Deliver ONB-04: replace the empty dashboard's graveyard of '--' tiles with a single calm, guided first-run — one primary "Import sleep" action plus the sleep-only privacy promise that also primes the permission ask before the OS prompt. Build it as a modular widget so Phase 11's hierarchy/insights work extends it instead of colliding.

Purpose: The first real screen after onboarding should offer one obvious next step, not blank placeholders (per 10-CONTEXT ONB-04). Keep the score ring / metric hierarchy / insights OUT of scope — those are Phase 11.
Output: A modular PatientFirstRun widget wired into the empty dashboard, with the existing data dashboard untouched once summaries exist.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-brand-arrival-and-guided-onboarding/10-CONTEXT.md
@lib/screens/patient_dashboard.dart
@lib/screens/common_widgets.dart
@lib/theme/app_theme.dart
@lib/theme/tokens.dart
</context>

## Artifacts this phase produces

- Plan 10-01: native web loader, `web/nid-brand-mark.png`, `lib/screens/brand_splash.dart`, in-memory splash flag, `showSplash` seam, `test/brand_splash_test.dart`.
- Plan 10-02: reworked `_OnboardingScreen`/`_HeroCopy` and `_PatientOnboardingScreen`; copy `Get started`, `I'm a clinician`, expectation bullets, `Skip for now`.
- Plan 10-03 (this plan): `lib/screens/patient_first_run.dart` (PatientFirstRun) + first-run copy constants (`Start with last night's sleep`, `Import sleep`, sleep-only priming with `never your journal`).

<tasks>

<task type="auto">
  <name>Task 1: Modular PatientFirstRun widget + empty-dashboard wiring</name>
  <read_first>lib/screens/patient_dashboard.dart lines 15-123 (build: metric Wrap + Import button + trend section), 125-171 (permission label/message/tone helpers); lib/screens/common_widgets.dart lines 384-420 (EmptyState), 67-94 (BrandMark); lib/theme/tokens.dart; lib/state/app_state.dart lines 198-224 (importMockSleep), 47-50 (healthPermission getters), 50 (isBusy)</read_first>
  <files>lib/screens/patient_first_run.dart, lib/screens/patient_dashboard.dart</files>
  <action>Create `lib/screens/patient_first_run.dart` exposing `PatientFirstRun extends StatelessWidget` taking `NguyenInDoubtState state`. It renders one calm guided hero card (reuse SectionCard/EmptyState idiom + BrandMark or a bedtime icon; weights <= w700; palette tokens only): a headline `Start with last night's sleep`, a body that states the sleep-only promise and primes the permission ask — it MUST contain the phrase `never your journal` and communicate that import requests sleep-only access before any OS prompt — and a single primary `FilledButton.icon` labelled `Import sleep` wired to `state.importMockSleep`, disabled when `state.isBusy` or `state.healthPermissionStatus == HealthPermissionStatus.unavailable` (mirror the existing Import button's disable + spinner logic). Keep the widget self-contained and modular (documented as the Phase 10 first-run layer that Phase 11 extends). In patient_dashboard.dart build(): when `state.summaries.isEmpty`, render `PatientFirstRun(state: state)` in place of BOTH the three `_MetricTile` Wrap and the Sleep-trend `SectionCard` (demote/hide the empty '--' tiles and the empty trend until there is data); keep the `BrandHeader` ('Morning check-in') and the `_ConsentLifecycleCard` SectionCard exactly as-is. When `state.summaries` is non-empty, render the existing metric tiles + trend section unchanged. Do not add a score ring, metric hierarchy, or insight lines (Phase 11).</action>
  <verify>
    <automated>grep -q "never your journal" lib/screens/patient_first_run.dart && grep -q "Import sleep" lib/screens/patient_first_run.dart && flutter analyze lib/screens/patient_first_run.dart lib/screens/patient_dashboard.dart</automated>
  </verify>
  <acceptance_criteria>
    - Empty state shows one guided hero with a single 'Import sleep' primary action and the sleep-only promise ('never your journal').
    - The '--' metric tiles and empty trend are hidden while summaries are empty; BrandHeader and consent card remain.
    - With data, the original metrics + trend render unchanged.
    - PatientFirstRun is a standalone modular widget (Phase 11 can extend it).
  </acceptance_criteria>
  <done>PatientFirstRun renders the guided empty first-run; patient_dashboard swaps to it when summaries are empty and keeps the data dashboard otherwise.</done>
</task>

<task type="auto">
  <name>Task 2: Update the coupled empty-state test assertions</name>
  <read_first>test/widget_test.dart lines 14-47 (first test: empty-state assertions + Import tap + post-import assertions), line 105 (reset test empty-state assertion)</read_first>
  <files>test/widget_test.dart</files>
  <action>In the first test ('patient can sign up and import mock sleep data'), replace the now-removed empty-state assertions with the first-run ones: change `find.text('No sleep samples yet')` (line ~34), `find.text('sleep permission needed')` (line ~35), and `find.text('Import requests sleep-only access.')` (line ~36) to assert the guided first-run — `find.text("Start with last night's sleep")` findsOneWidget and `find.textContaining('never your journal')` findsOneWidget. Change the import tap target from `find.widgetWithText(FilledButton, 'Import')` to `find.widgetWithText(FilledButton, 'Import sleep')` (keep the existing ensureVisible + tap). Keep the post-import assertions `find.text('sleep access ready')` findsOneWidget (the trend card's permission pill after data) and change the post-import `find.text('No sleep samples yet')` findsNothing to `find.text("Start with last night's sleep")` findsNothing. In the reset test, update the trailing `find.text('No sleep samples yet')` findsNothing (line ~105) to `find.text("Start with last night's sleep")` findsNothing. Do not touch the demo-notice, consent, safety, or clinician assertions.</action>
  <verify>
    <automated>flutter test test/widget_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - The empty-state flow asserts the new guided first-run copy and taps 'Import sleep'.
    - The post-import flow still proves data appears ('sleep access ready') and the first-run hero is gone.
    - The full widget_test suite is green.
  </acceptance_criteria>
  <done>widget_test.dart empty-state + post-import assertions match the guided first-run; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → local app state | The 'Import sleep' action triggers the existing sleep-import path (device-local demo / sleep-only permission); no journal data is read or exposed here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-03-ID | Information Disclosure | first-run priming copy | low | mitigate | Copy states the truth — sleep-only, never the journal — and does not overstate access; it primes the sleep-only permission ask before the OS prompt. |
| T-10-03-TMP | Tampering | Import sleep action | low | accept | The button calls the existing `importMockSleep`/repository path, which is already privacy-guarded (sleep-only); no new data path or capability is introduced. |
</threat_model>

<verification>
Plan-level bar (run all, must pass):
- `dart format lib test`
- `flutter analyze` (no issues)
- `flutter test` (all green)
- `flutter build web`

Human-check (advisory): sign up as a patient and confirm the empty dashboard shows a single calm 'Import sleep' hero with the sleep-only promise (no '--' tiles), and that after import the normal metrics + trend chart appear.
</verification>

<success_criteria>
- The empty first-run dashboard presents one clear primary action ('Import sleep') with sleep-only permission priming before any OS prompt (ONB-04).
- The '--' placeholder graveyard is gone; the first-run layer is modular for Phase 11.
</success_criteria>

<output>
Create `.planning/phases/10-brand-arrival-and-guided-onboarding/10-03-SUMMARY.md` when done.
</output>
