---
phase: 10-brand-arrival-and-guided-onboarding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/index.html
  - web/nid-brand-mark.png
  - lib/main.dart
  - lib/screens/brand_splash.dart
  - lib/state/app_state.dart
  - test/widget_test.dart
  - test/journal_delete_test.dart
  - test/clinician_affordance_test.dart
  - test/brand_splash_test.dart
autonomous: true
requirements: [ONB-01]
user_setup: []

must_haves:
  truths:
    - "Cold-start (web) paints an on-brand fog #F6F7F1 ground with a centered mark instead of a blank white frame, and it is removed once Flutter's first frame paints (per D ONB-01)."
    - "A gentle animated brand-intro splash (BrandMark fade -> soft bloom -> wordmark fade-up -> cross-fade to onboarding) plays once per cold launch, guarded by an in-memory bool — never a persisted counter and never analytics (per D ONB-01)."
    - "The splash is instant/skipped under widget tests via a showSplash seam, so existing pumpAndSettle-from-boot tests do not hang."
    - "The splash cross-fades out; it never pops (graceful-transitions rule)."
  artifacts:
    - "web/index.html native loader block (fog ground + centered mark, first-frame removal)"
    - "web/nid-brand-mark.png (loader-visible copy of the square mark)"
    - "lib/screens/brand_splash.dart — BrandSplashGate widget"
    - "in-memory splashHasPlayed flag on NguyenInDoubtState"
    - "NguyenInDoubtApp({showSplash}) test seam"
    - "test/brand_splash_test.dart"
  key_links:
    - "lib/main.dart wires home to the splash gate when showSplash is true, else AppShell"
    - "All three app-pumping test files pass showSplash: false"
    - "flutter-first-frame event removes the native loader"
---

<objective>
Fix the app's worst first impression: the blank white web boot frame and an un-branded cold-start. Deliver ONB-01 — a native web loader that fills the pre-Flutter paint with the brand fog ground + mark, and an in-Flutter animated brand-intro splash that plays once per cold launch and gently cross-fades into onboarding. Add the test seam that makes the splash instant under widget tests so the whole existing suite keeps passing.

Purpose: The first minute must feel calm and branded (per 10-CONTEXT ONB-01). This plan is foundational for the phase: the splash gate wraps AppShell and the `showSplash` seam must exist before Plans 10-02/10-03 (which also pump the app in tests) run.
Output: Native web loader, BrandSplashGate widget, in-memory splash-shown flag, the `showSplash` seam, and the full test suite migrated to skip the splash.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-brand-arrival-and-guided-onboarding/10-CONTEXT.md
@web/index.html
@lib/main.dart
@lib/state/app_state.dart
@lib/screens/common_widgets.dart
@lib/theme/app_theme.dart
@lib/theme/tokens.dart
</context>

## Artifacts this phase produces

- Plan 10-01: native web loader (web/index.html), `web/nid-brand-mark.png`, `lib/screens/brand_splash.dart` (splash gate), in-memory `splashHasPlayed` flag on app_state, `NguyenInDoubtApp({showSplash})` seam, `test/brand_splash_test.dart`.
- Plan 10-02: reworked `_OnboardingScreen`/`_HeroCopy` and `_PatientOnboardingScreen` (new copy constants: `Get started`, `I'm a clinician`, three expectation bullets, `Skip for now`).
- Plan 10-03: `lib/screens/patient_first_run.dart` modular first-run widget + new first-run copy constants.

<tasks>

<task type="auto">
  <name>Task 1: Native on-brand web loader in index.html</name>
  <read_first>web/index.html (whole file, 47 lines); pubspec.yaml lines 57-77 (asset declarations); lib/theme/app_theme.dart line 9 (NidColors.fog = 0xFFF6F7F1)</read_first>
  <files>web/index.html, web/nid-brand-mark.png</files>
  <action>Copy the existing square mark `assets/brand/nguyenindoubt-square-mark.png` to `web/nid-brand-mark.png` (a same-origin, pre-Flutter-visible copy; the loader runs before Flutter assets are available). In `web/index.html`, add a loader element inside `<body>` before the `flutter_bootstrap.js` script: a full-viewport centered container with background `#F6F7F1` (brand fog) holding an `<img src="nid-brand-mark.png">` sized ~96px with a soft rounded badge and a very gentle fade/pulse via inline `<style>` (calm, no bounce). Add a small inline `<script>` that removes the loader element when Flutter's first frame paints — listen for the `flutter-first-frame` event on `window` (with a defensive fallback timeout ~8s in case the event is missed). Keep all markup static (no dynamic string interpolation, no external URLs). Do not alter the existing `<base href>`, meta, or bootstrap tags.</action>
  <verify>
    <automated>test -f web/nid-brand-mark.png && grep -q 'F6F7F1' web/index.html && grep -q 'nid-brand-mark.png' web/index.html && grep -q 'flutter-first-frame' web/index.html && echo LOADER_OK</automated>
  </verify>
  <acceptance_criteria>
    - The pre-Flutter paint shows the fog ground + centered mark, not blank white.
    - The loader is removed on `flutter-first-frame` (with a safety-timeout fallback).
    - `flutter build web` still succeeds (checked at the plan-level bar).
  </acceptance_criteria>
  <done>web/index.html contains a static fog-ground loader referencing web/nid-brand-mark.png that self-removes on first frame; the mark file exists in web/.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BrandSplashGate widget + in-memory splash flag + showSplash seam</name>
  <read_first>lib/main.dart (whole file); lib/state/app_state.dart lines 7-55 (fields/getters, constructor); lib/screens/common_widgets.dart lines 67-94 (BrandMark); lib/screens/app_shell.dart lines 12-44 (AppShell signature + signed-out onboarding); lib/theme/app_theme.dart (palette: fog/canopy/ember)</read_first>
  <behavior>
    - With showSplash false: BrandSplashGate renders AppShell immediately — no AnimationController, no pending timers, so a widget test settles instantly.
    - With showSplash true and splashHasPlayed false: the splash view renders first (BrandMark fades in, soft bloom/scale, wordmark "NguyenInDoubt" fades up beneath), the animation is finite (~3.5s, no infinite repeat), then it cross-fades (AnimatedSwitcher) to AppShell and sets splashHasPlayed true.
    - With showSplash true and splashHasPlayed already true: renders AppShell immediately (once-per-cold-launch guarantee).
  </behavior>
  <files>lib/state/app_state.dart, lib/screens/brand_splash.dart, lib/main.dart</files>
  <action>Add a plain in-memory field `bool splashHasPlayed = false;` and a `void markSplashPlayed() { splashHasPlayed = true; }` to NguyenInDoubtState — NOT persisted, NOT a counter, NO analytics, and it does not need to notifyListeners. Create `lib/screens/brand_splash.dart` exposing `BrandSplashGate extends StatefulWidget` taking `NguyenInDoubtState state` and `bool enabled`. In its State: if `!enabled || state.splashHasPlayed`, build AppShell directly. Otherwise drive a single finite AnimationController (~3500ms; stage the BrandMark fade-in ~0-40%, a soft scale/bloom mid, and the wordmark FadeTransition up ~55-100%) using palette tokens (fog ground, canopy ink, ember accent; weights <= w700; reuse BrandMark). Wrap the child in an AnimatedSwitcher with a fade transition; on controller completion call `state.markSplashPlayed()` and setState to swap the child to AppShell (cross-fade, never pop). Use only core Flutter animation APIs — no new dependencies. In lib/main.dart add `final bool showSplash;` to NguyenInDoubtApp with `this.showSplash = true` default, and set `home: BrandSplashGate(state: state, enabled: showSplash)`.</action>
  <verify>
    <automated>flutter analyze lib/screens/brand_splash.dart lib/main.dart lib/state/app_state.dart</automated>
  </verify>
  <acceptance_criteria>
    - `NguyenInDoubtApp` accepts `showSplash` (default true); production `main()` gets the splash.
    - The splash animation is finite and one-shot; it cross-fades to AppShell.
    - splashHasPlayed is in-memory only (grep shows no SharedPreferences / persisted key for it).
  </acceptance_criteria>
  <done>BrandSplashGate plays a finite gentle splash once, then cross-fades to AppShell; showSplash seam wired through main.dart; in-memory flag added.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate the app-pumping tests to skip the splash + splash behavior test</name>
  <read_first>test/widget_test.dart (all NguyenInDoubtApp(state:...) construction sites — lines 25, 60, 115, 146, 177, 224, 259, 294, 305, 328, 340, 368); test/journal_delete_test.dart line 102; test/clinician_affordance_test.dart line 30</read_first>
  <files>test/widget_test.dart, test/journal_delete_test.dart, test/clinician_affordance_test.dart, test/brand_splash_test.dart</files>
  <action>Add `showSplash: false` to every `NguyenInDoubtApp(state: ...)` construction across test/widget_test.dart (12 sites), test/journal_delete_test.dart (1 site, line ~102), and test/clinician_affordance_test.dart (1 site, line ~30). Do not change any assertions or labels in this task (label renames are Plan 10-02). Create `test/brand_splash_test.dart` with two widget tests: (1) with `showSplash: false`, after `pumpWidget` + `pumpAndSettle` the onboarding surface is reachable immediately and no splash-only widget remains; (2) with `showSplash: true` on a fresh state, the splash renders first (assert a splash marker is present before settle), then `pumpAndSettle` completes without timeout and AppShell's signed-out onboarding is shown, and `state.splashHasPlayed` is true. Keep the second test resilient (bounded pumpAndSettle) since the animation is finite.</action>
  <verify>
    <automated>flutter test test/widget_test.dart test/journal_delete_test.dart test/clinician_affordance_test.dart test/brand_splash_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - The full existing suite passes and does not hang (splash skipped under tests).
    - brand_splash_test proves both the skipped path and the one-shot animated path settle.
  </acceptance_criteria>
  <done>All app-pumping tests pass showSplash: false and stay green; brand_splash_test covers skip + play-once behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → static web loader | Pre-Flutter HTML/CSS/JS paints before the engine boots; no user input crosses here |
| dependency surface | pub packages the app pulls in |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01-INJ | Tampering | web/index.html loader script | low | mitigate | Loader markup is fully static, references a same-origin local image, and performs no dynamic string interpolation or remote fetch. |
| T-10-01-ID | Information Disclosure | splash/loader surface | low | accept | Loader and splash render only brand assets and the app name — no PHI, sleep, or journal data is shown pre-auth. |
| T-10-01-SC | Tampering | pub dependency graph | low | accept | No new dependencies added; splash uses core Flutter animation APIs and the loader is plain HTML — verify pubspec.yaml dependencies are unchanged. |
</threat_model>

<verification>
Plan-level bar (run all, must pass):
- `dart format lib test`
- `flutter analyze` (no issues)
- `flutter test` (all green — must NOT hang; the seam guarantees the splash is skipped)
- `flutter build web` (the loader change must not break the build)

Human-check (visual, advisory — automated tests cannot judge felt motion): run `flutter run -d chrome` on a stable port and confirm the cold-start shows the fog+mark loader (not blank white), the splash fades/blooms gently and cross-fades into onboarding once, and never pops.
</verification>

<success_criteria>
- Cold-start is on-brand (native loader + one-shot animated splash), replacing the blank boot frame (ONB-01).
- Splash plays once per cold launch via an in-memory bool (no persisted counter, no analytics).
- The `showSplash` seam skips the splash under tests; the full suite is green and does not hang.
</success_criteria>

<output>
Create `.planning/phases/10-brand-arrival-and-guided-onboarding/10-01-SUMMARY.md` when done.
</output>
