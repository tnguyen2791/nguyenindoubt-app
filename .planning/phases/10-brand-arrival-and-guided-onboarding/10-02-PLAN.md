---
phase: 10-brand-arrival-and-guided-onboarding
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - lib/screens/app_shell.dart
  - test/widget_test.dart
  - test/journal_delete_test.dart
  - test/clinician_affordance_test.dart
autonomous: true
requirements: [ONB-02, ONB-03]
user_setup: []

must_haves:
  truths:
    - "The welcome leads with a single patient-first primary action ('Get started'); there is exactly one primary button (per D ONB-02)."
    - "Clinician access is de-emphasized to a low-key text link (\"I'm a clinician\") but remains reachable and still enters the clinician demo (per D ONB-02)."
    - "The required local-only disclosure stays present but reads as calm secondary copy, not bolded hero copy (relocated, not deleted) (per D ONB-02)."
    - "On narrow (mobile) layout the stag gets top billing — it appears above the hero copy/CTAs (per D ONB-02)."
    - "Patient onboarding shows three one-line expectation bullets: sleep imported privately / a journal only you can read / guides with room for doubt (per D ONB-03)."
    - "An empty or whitespace-only name cannot submit — Continue is disabled — and an explicit 'Skip for now' proceeds with the demo fallback name (per D ONB-03)."
    - "The onboarding disclosure keeps the substring 'Account-backed production storage is not enabled' (test-asserted)."
  artifacts:
    - "Reworked _OnboardingScreen / _HeroCopy in lib/screens/app_shell.dart"
    - "Reworked _PatientOnboardingScreen in lib/screens/app_shell.dart"
    - "Updated CTA taps/asserts across the three app-pumping test files"
    - "A new name-guard widget test"
  key_links:
    - "The de-emphasized clinician link still calls onClinician (continueAsClinicianDemo)"
    - "Continue / Skip both wire to onComplete -> state.completePatientOnboarding (which already trims + falls back to the demo name)"
---

<objective>
Deliver ONB-02 and ONB-03: reframe the welcome to be patient-first (one primary action, clinician access demoted to a calm text link, the local-only disclosure lowered to secondary copy, stag given top billing on mobile), and make patient onboarding set expectations with three bullets while guarding the name field. Update the coupled test suite to the new labels and add a guard test.

Purpose: The first screen should invite the patient in without the "graveyard of choices" feel, while keeping the clinician demo reachable and the privacy disclosure honest but calm (per 10-CONTEXT ONB-02/ONB-03).
Output: A reworked onboarding/welcome surface and a green suite on the new vocabulary.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-brand-arrival-and-guided-onboarding/10-CONTEXT.md
@lib/screens/app_shell.dart
@lib/screens/common_widgets.dart
@lib/theme/app_theme.dart
@lib/theme/tokens.dart
</context>

## Artifacts this phase produces

- Plan 10-01: native web loader, `web/nid-brand-mark.png`, `lib/screens/brand_splash.dart`, in-memory splash flag, `showSplash` seam, `test/brand_splash_test.dart`.
- Plan 10-02 (this plan): reworked `_OnboardingScreen`/`_HeroCopy` and `_PatientOnboardingScreen`; new copy constants `Get started`, `I'm a clinician`, three expectation bullets, `Skip for now`; appbar clinician role label shortened to `Clinician demo`.
- Plan 10-03: `lib/screens/patient_first_run.dart` modular first-run widget + first-run copy.

<tasks>

<task type="auto">
  <name>Task 1: Patient-first welcome reframe (_OnboardingScreen / _HeroCopy)</name>
  <read_first>lib/screens/app_shell.dart lines 255-316 (_AppBar roleLabel), 318-416 (_OnboardingScreen, _HeroCopy), 503-535 (_StagPanel); lib/theme/app_theme.dart (palette + text ladder); lib/theme/tokens.dart (NidSpace)</read_first>
  <files>lib/screens/app_shell.dart, test/widget_test.dart, test/journal_delete_test.dart, test/clinician_affordance_test.dart</files>
  <action>In _HeroCopy: make the patient path the sole primary — a single `FilledButton.icon` labelled `Get started` wired to `onPatient`. Replace the `OutlinedButton` "Clinician demo override" with a de-emphasized `TextButton` at the bottom of the hero labelled `I'm a clinician` (use double quotes in Dart for the apostrophe), still wired to `onClinician` — keep it clearly reachable, just visually quiet (label style, no filled/outlined emphasis). Relocate the existing hero disclosure line ("Demo auth is local to this device. Firebase sign-in is not live yet.") from its moss w600 treatment to calm secondary copy: smaller (bodySmall), muted (NidColors.slate), placed below the CTAs, weight <= w600 (lower its emotional volume; do not delete it). In _OnboardingScreen narrow (non-wide) branch, give the stag top billing — order the `_StagPanel` (or a shorter banner crop of it) ABOVE `_HeroCopy` in the ListView so it is the first element on mobile. Leave the wide (Row) layout hero-left/stag-right. In _AppBar, change the clinician `roleLabel` from `Clinician demo override` to `Clinician demo` for consistent vocabulary. Then update the CTA references in tests: replace every `find.text('Patient sign up')` with `find.text('Get started')` and every `find.text('Clinician demo override')` with `find.text("I'm a clinician")` across test/widget_test.dart (both the boot assertions and the two `_completePatientOnboarding` helpers), test/journal_delete_test.dart (its local `_completePatientOnboarding`, line ~177), and test/clinician_affordance_test.dart (line ~32).</action>
  <verify>
    <automated>grep -q "Get started" lib/screens/app_shell.dart && grep -q "I'm a clinician" lib/screens/app_shell.dart && flutter test test/widget_test.dart test/journal_delete_test.dart test/clinician_affordance_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one primary (FilledButton) action on the welcome; clinician entry is a quiet TextButton that still enters the clinician demo.
    - On phone width the stag renders above the primary CTA (top billing).
    - The local-only disclosure is present as calm secondary copy.
    - The full suite is green on the new labels.
  </acceptance_criteria>
  <done>Welcome is patient-first with a single primary, a de-emphasized reachable clinician link, relocated calm disclosure, and stag top billing on mobile; tests updated and green.</done>
</task>

<task type="auto">
  <name>Task 2: Onboarding expectations + name guard (_PatientOnboardingScreen)</name>
  <read_first>lib/screens/app_shell.dart lines 418-501 (_PatientOnboardingScreen); lib/state/app_state.dart lines 107-117 (completePatientOnboarding trims + falls back to demo name); test/widget_test.dart lines 109-136 (public-demo test enters a name then taps Continue) and 394-404 (helper)</read_first>
  <files>lib/screens/app_shell.dart, test/widget_test.dart</files>
  <action>In _PatientOnboardingScreen, add three one-line "what this app does" bullets beneath the disclosure paragraph (keep the paragraph and its substring 'Account-backed production storage is not enabled'): `Sleep, privately imported`, `A journal only you can read`, `Guides, with room for doubt` — each a small calm row (icon + label, weight <= w600), no carousel, one screen. Guard the name field: track the controller text, compute `trimmed = _nameController.text.trim()`; disable the `Continue` FilledButton (onPressed null) whenever `trimmed` is empty, and add an explicit `TextButton` labelled `Skip for now` that calls `widget.onComplete('')` (state.completePatientOnboarding already trims empty -> demo fallback name). Wire the TextField `onChanged` to `setState` so the button enables/disables live; keep `onSubmitted` calling onComplete only when non-empty. Add a widget test to test/widget_test.dart: after tapping `Get started`, with an empty/whitespace name the `Continue` button is disabled (its `onPressed` is null); entering a real name enables it; `Skip for now` proceeds to the patient dashboard using the demo fallback name.</action>
  <verify>
    <automated>flutter test test/widget_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - Three expectation bullets appear on the single onboarding screen.
    - Continue is disabled for empty/whitespace names and enabled once a real name is entered.
    - 'Skip for now' proceeds with the demo fallback name; existing name flows still pass.
    - The 'Account-backed production storage is not enabled' disclosure substring remains.
  </acceptance_criteria>
  <done>Onboarding sets expectations with three bullets and guards the name field (disabled Continue + Skip fallback); new guard test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → local app state | The display-name free-text field is the only user input; it is stored device-local (demo, in-memory/SharedPreferences), never sent to a server |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-02-INJ | Tampering | display-name TextField | low | mitigate | Input is trimmed before use and rendered only through Flutter `Text` (no HTML/script surface); empty/whitespace cannot submit. |
| T-10-02-ID | Information Disclosure | relocated local-only disclosure | low | accept | The privacy disclosure is relocated/restyled but retained (present as calm secondary copy), preserving the transparency guarantee; test-asserted substring kept. |
| T-10-02-EOP | Elevation of Privilege | de-emphasized clinician link | low | accept | The clinician link enters the same device-local demo override path as before (continueAsClinicianDemo) — no new privilege or trusted-role bypass is introduced. |
</threat_model>

<verification>
Plan-level bar (run all, must pass):
- `dart format lib test`
- `flutter analyze` (no issues)
- `flutter test` (all green)
- `flutter build web`

Human-check (advisory): confirm the welcome reads patient-first (one primary "Get started"), the clinician link is quiet but tappable, the disclosure is calm secondary text, and on a phone-width window the stag sits above the primary action.
</verification>

<success_criteria>
- Welcome leads with a single patient-first primary; clinician demo de-emphasized but reachable; local-only disclosure calm secondary copy (ONB-02).
- Onboarding communicates sleep/journal/privacy and rejects empty/invalid names with a Skip fallback (ONB-03).
</success_criteria>

<output>
Create `.planning/phases/10-brand-arrival-and-guided-onboarding/10-02-SUMMARY.md` when done.
</output>
