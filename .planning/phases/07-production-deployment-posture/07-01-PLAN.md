# Phase 7 Plan 07-01: Production Deployment Posture

## Goal

Close the v1 GSD milestone with explicit production posture, demo data
expectations, release notes, and verification evidence.

## Tasks

1. Add data posture documentation for demo mode, production mode, retention,
   export, deletion, and compliance blockers.
2. Add v1 release notes that call out privacy-sensitive changes.
3. Update README with run/deploy verification commands and data-mode summary.
4. Tighten in-app demo banner and onboarding copy so public users see local data
   mode and production limitations.
5. Add or update widget tests for public demo disclosure.
6. Run `flutter analyze`, `flutter test`, Firestore rules tests, and
   `flutter build web`.
7. Mark PROD requirements complete and write verification/summary artifacts.

## Verification

- `flutter analyze`
- `flutter test`
- `npm run test:firestore-rules`
- `flutter build web`
