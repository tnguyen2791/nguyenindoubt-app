---
phase: 11-insightful-data-displays
plan: 03
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - lib/screens/clinician_dashboard.dart
  - test/clinician_affordance_test.dart
autonomous: true
requirements: [INS-05]
user_setup: []

must_haves:
  truths:
    - "The clinician patient-detail card shows a directional stat-delta summary — average sleep with a delta vs the prior week, a night-to-night variability descriptor, and nights-with-data — instead of raw counts (per D INS-05)."
    - "The consent disclosure 'Visible: sleep samples, daily summaries, trend flags. Hidden: journal entries, drafts, private reflections.' remains byte-for-byte verbatim on the clinician surface (per D INS-05 — it is disclosure, not a metric)."
    - "Everything the clinician sees still derives exclusively from the consent-gated PatientSleepBundle.summaries — no new data kinds cross the consent boundary, and journal data remains structurally unreachable."
    - "The directional summary is deterministic against the seeded demo data and pinned by a test in clinician_affordance_test.dart in the same plan."
  artifacts:
    - "lib/screens/clinician_dashboard.dart — _PatientDetail stat area rebuilt as StatDeltaRow column fed by clinicianWeekSummary; raw count tiles removed"
    - "test/clinician_affordance_test.dart — new directional-summary test (delta words present, raw count label absent)"
  key_links:
    - "clinician_dashboard.dart -> clinicianWeekSummary in lib/services/sleep_insights.dart (computed from bundle.summaries only)"
    - "clinician_dashboard.dart -> StatDeltaRow in lib/screens/data_displays.dart (the design's stat-delta idiom)"
---

<objective>
Replace the clinician detail's raw count tiles — including the bare sample-count stat — with the design's directional stat-delta summary: average sleep with a delta vs the prior week, a night-to-night variability descriptor, and nights-with-data, all computed on device from the already-consent-gated sleep summaries, with the coupled clinician test suite updated in the same plan.

Purpose: INS-05 clinician side — deltas and variability words replace raw counts, staying non-diagnostic and inside the existing privacy contract (sleep summaries only).
Output: Rebuilt `_PatientDetail` stat area in `clinician_dashboard.dart` + a new directional-summary test in `clinician_affordance_test.dart`; full phase verification bar green.
</objective>

## Phase Goal

**As a** patient (and their linked clinician), **I want to** see honest, hierarchical sleep insights — a real score, truthful charts, and gentle observations — **so that** the data feels trustworthy instead of raw and redundant. (Derived 1:1 from the ROADMAP Phase 11 goal; no scope added.)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-insightful-data-displays/11-CONTEXT.md
@.planning/phases/11-insightful-data-displays/11-DESIGN-SPEC.md
@.planning/phases/11-insightful-data-displays/11-01-SUMMARY.md
@lib/screens/clinician_dashboard.dart
@lib/services/sleep_insights.dart
@lib/screens/data_displays.dart
</context>

## Artifacts this phase produces

- Plan 11-01: `NidStateColors` + `NidColors.faint` + sleep-hours ramp in `lib/theme/app_theme.dart`; `lib/services/sleep_insights.dart` (`StateTone`, `ScoreContributor`, `SleepScore`, `ClinicianWeekSummary`, `computeSleepScore`, `insightLine`, `consistencyCaption`, `weekDeltaLabel`, `clinicianWeekSummary`); `lib/screens/data_displays.dart` (`ScoreRing`, `ContributorBar`, `InfoTip`, `StatDeltaRow`, `nidToneColor`, `dayLetter`); honest `SleepTrendBars`; `test/services/sleep_insights_test.dart`; `test/data_displays_test.dart`.
- Plan 11-02: rebuilt with-data `patient_dashboard.dart` (`_GreetingBlock`, `_ScoreHeroCard`, `_MiniMetricCard`, retitled trend card + consistency caption, consent card last); updated `test/widget_test.dart` + phone-width hierarchy test.
- Plan 11-03 (this plan): `clinician_dashboard.dart` `_PatientDetail` directional summary (StatDeltaRow column: 'Avg sleep' + delta vs prior week, 'Night-to-night' variability, 'Nights with data'); removal of the raw count tiles and the `_ClinicianMetric` widget; new directional-summary test in `test/clinician_affordance_test.dart`.

<tasks>

<task type="auto">
  <name>Task 1: Directional stat-delta summary in the clinician patient detail</name>
  <read_first>.planning/phases/11-insightful-data-displays/11-DESIGN-SPEC.md sections "Stat/delta rows" and "Copy bank" (the stat-delta idiom: name 14px/600 + sub 12px faint, value 17px/700 + delta 12px/600 moss, ember when flagged); lib/screens/clinician_dashboard.dart (entire file — _PatientDetail 170-262 with the inline avg/shortNights computation 187-196, the stat Wrap 220-237 including the raw count tile at 232-235, _ClinicianMetric 264-298, and the verbatim visibility copy at 244-258); lib/services/sleep_insights.dart (ClinicianWeekSummary + clinicianWeekSummary from plan 11-01); lib/screens/data_displays.dart (StatDeltaRow); lib/data/seed_data.dart lines 64-89 (seeded linked-patient durations 6.2/6.5/7.0/7.2/5.9/6.8/7.6 — exactly 7 nights)</read_first>
  <files>lib/screens/clinician_dashboard.dart</files>
  <action>In `_PatientDetail.build` (per D INS-05): delete the inline `average` and `shortNights` computations (lines 187-196), the stat `Wrap` of three `_ClinicianMetric` tiles (lines 220-237) including the raw sample-count tile (lines 232-235), and the now-unused `_ClinicianMetric` class (lines 264-298). In their place compute `final week = clinicianWeekSummary(bundle!.summaries);` (import `../services/sleep_insights.dart`) and render a Column of three `StatDeltaRow`s (import `data_displays.dart`), separated by NidSpace.m gaps:
1. `StatDeltaRow(name: 'Avg sleep', sub: 'last 7 nights', value: week.avgLabel, delta: week.deltaLabel, flagged: week.deltaFlagged)` — with the 7-night seed this deterministically renders '6.7h' and 'no prior week yet'.
2. `StatDeltaRow(name: 'Night-to-night', sub: 'variability', value: week.variabilityValue, delta: week.variabilityWord, flagged: week.variabilityFlagged)` — seeded data renders '±0.5h' / 'steady nights'.
3. `StatDeltaRow(name: 'Nights with data', sub: 'past week', value: week.nightsLabel, delta: 'sleep summaries only')` — the delta slot carries the positive scope framing.
Everything else in the file stays byte-for-byte: the `BrandHeader` ('Accepted invites only. Sleep summaries, never journals.'), `_PatientList` and its SAFE-02 affordance logic, the 'journal private' StatusPill, `SleepTrendBars(summaries: summaries)` (already honest from plan 11-01), and the visibility card with its VERBATIM copy 'Visible: sleep samples, daily summaries, trend flags. Hidden: journal entries, drafts, private reflections.' — that sentence is consent disclosure, not a metric, and must not change by a single character. No new data may be read: the summary derives only from `bundle!.summaries` (consent-gated sleep summaries); do not touch `bundle!.samples` or add any repository call. Do not reference the removed tile labels in comments. Directional words stay observational and non-diagnostic — no risk language, no person-labeling.</action>
  <verify>
    <automated>flutter analyze lib/screens/clinician_dashboard.dart && ! grep -qF "'samples'" lib/screens/clinician_dashboard.dart && ! grep -q "_ClinicianMetric" lib/screens/clinician_dashboard.dart && grep -q "clinicianWeekSummary" lib/screens/clinician_dashboard.dart && grep -qF "Visible: sleep samples, daily summaries, trend flags. Hidden: journal entries, drafts, private reflections." lib/screens/clinician_dashboard.dart</automated>
  </verify>
  <acceptance_criteria>
    - clinician_dashboard.dart contains no raw count stat: the quoted label literal 'samples' and the _ClinicianMetric class are gone; the three StatDeltaRows render avg+delta, variability, and nights-with-data from clinicianWeekSummary.
    - The visibility disclosure sentence is byte-for-byte unchanged (grep -F exact match passes).
    - No new repository/provider calls and no use of bundle samples beyond what existed; the summary is a pure function of bundle.summaries.
    - With the seeded demo patient (7 nights), the detail deterministically shows '6.7h', 'no prior week yet', '±0.5h', 'steady nights', and '7 of 7 nights'.
  </acceptance_criteria>
  <done>The clinician sees a directional, non-diagnostic week summary in the design's stat-delta idiom instead of raw counts, within the unchanged sleep-summaries-only privacy contract.</done>
</task>

<task type="auto">
  <name>Task 2: Pin the directional summary in clinician_affordance_test + full phase verification bar</name>
  <read_first>test/clinician_affordance_test.dart (entire file — the SAFE-02 affordance test whose finds must keep passing; note the pump pattern with showSplash: false and the 1000x1200 view sizing); lib/screens/clinician_dashboard.dart (as rebuilt by Task 1); test/widget_test.dart lines 272-327 (the clinician privacy + lifecycle tests that must stay green unchanged)</read_first>
  <files>test/clinician_affordance_test.dart</files>
  <action>Add a second `testWidgets` to `test/clinician_affordance_test.dart` named 'clinician summary is directional, never a raw sample count': reuse the file's existing pump pattern — 1000x1200 view with teardowns, fresh `NguyenInDoubtState(repository: InMemoryAppRepository(), healthDataProvider: MockHealthDataProvider())`, `NguyenInDoubtApp(state: state, showSplash: false)` (the Phase 10 seam), pumpAndSettle, tap "I'm a clinician", pumpAndSettle. Clinician mode auto-selects the first linked patient, so the detail renders the seeded 7-night data. Assert: `find.text('SAMPLES')` findsNothing (the old uppercase tile label must not render); `find.text('Avg sleep')` findsOneWidget; `find.text('no prior week yet')` findsOneWidget (deterministic — exactly 7 seeded nights means no prior week); `find.text('steady nights')` findsOneWidget (seeded durations have sd ~0.55h); `find.text('7 of 7 nights')` findsOneWidget; `find.text('sleep summaries only')` findsOneWidget (positive scope framing); and scroll to + assert the verbatim disclosure still renders — `find.textContaining('Visible: sleep samples, daily summaries, trend flags.')` findsOneWidget (use scrollUntilVisible with the existing pattern from widget_test if off-screen). Do not modify the existing SAFE-02 affordance test — the invite-row finds (`NID-8274 - accepted`, chevron, opacity) are untouched by the detail rework and must pass as-is. Then run the full phase verification bar (dart format, analyze, full flutter test, flutter build web) and fix only formatting/analyze fallout from this phase's files — never loosen a privacy assertion.</action>
  <verify>
    <automated>flutter test test/clinician_affordance_test.dart && dart format --set-exit-if-changed lib test && flutter analyze && flutter test && flutter build web</automated>
  </verify>
  <acceptance_criteria>
    - The new test proves the directional summary (delta words, variability word, nights-with-data) renders and the raw count label does not, using showSplash: false.
    - The pre-existing SAFE-02 affordance test passes without modification.
    - The clinician privacy tests in widget_test.dart (verbatim 'Visible: sleep samples...' + 'Accepted invites only. Sleep summaries, never journals.') pass unchanged.
    - Full verification bar green: dart format 0 changed, flutter analyze no issues, flutter test all pass, flutter build web succeeds.
  </acceptance_criteria>
  <done>The clinician surface's directional summary is regression-pinned, raw counts are provably gone, and the whole phase passes the project verification bar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consent-gated sleep summaries -> clinician-visible derived stats | clinicianWeekSummary consumes only PatientSleepBundle.summaries, which the repository already gates on an accepted link + granted consent |
| clinician surface -> journal data | Unchanged: journal types never enter clinician screens or state; this plan adds no data path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-03-ID | Information Disclosure | clinician directional summary | medium | mitigate | Derived stats are computed exclusively from already-consented sleep summaries (bundle.summaries) via a DailySummary-only pure function — no new data kind crosses the consent boundary; the verbatim disclosure copy is grep- and test-pinned. |
| T-11-03-EoP | Elevation of Privilege | _PatientDetail rework | low | mitigate | No new repository calls; the existing accepted-link gate in getPatientSleepSummary remains the sole data source; acceptance criteria forbid new reads. |
| T-11-03-RP | Repudiation/misrepresentation | delta/variability wording | low | mitigate | Words are directional and observational ('steady nights', 'some variability', 'no prior week yet') — no diagnostic or risk framing; flagged state only shifts color to ember per the design idiom. |
| T-11-SC | Tampering | package installs | low | mitigate | No installs; pubspec.yaml unmodified. |
</threat_model>

<verification>
Plan-level bar (run all, must pass — this is also the phase close-out):
- `dart format lib test` (0 changed)
- `flutter analyze` (no issues)
- `flutter test` (all green)
- `flutter build web`

Human-check (advisory, end-of-phase): enter the clinician demo, open the accepted patient — the detail card shows Avg sleep with a delta line, a variability descriptor, and nights-with-data (no bare sample count), the honest ramp trend bars below, and the unchanged 'Visible: sleep samples...' disclosure.
</verification>

<success_criteria>
- Consistency/balance micro-insights (patient, plan 11-02) and a directional clinician summary (deltas, variability) replace raw counts like the sample tally (INS-05, ROADMAP SC-5).
- The clinician privacy contract is unchanged and test-pinned: sleep summaries only, verbatim disclosure copy, journals structurally unreachable.
</success_criteria>

<output>
Create `.planning/phases/11-insightful-data-displays/11-03-SUMMARY.md` when done.
</output>
