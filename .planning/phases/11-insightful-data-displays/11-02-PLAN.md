---
phase: 11-insightful-data-displays
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - lib/screens/patient_dashboard.dart
  - test/widget_test.dart
autonomous: true
requirements: [INS-01, INS-02, INS-04, INS-05]
user_setup: []

must_haves:
  truths:
    - "The with-data dashboard reads top-to-bottom: greeting/status block -> score hero card (ring + 3 contributors + verbatim 'not a diagnosis' headnote) -> two-up secondary mini-cards -> honest trend card with a micro-insight caption -> consent card last and visually quiet (per D INS-01, D INS-02)."
    - "The 0-100 proxy tile is gone from the UI and the connection-state tile no longer renders as a co-equal health metric; no sharing affordance appears in the greeting, hero, or metric cards (per D INS-01, D INS-02)."
    - "Exactly one gentle observational insight line appears — the greeting sub-line, computed on device (last-night-vs-baseline), strictly descriptive, never labeling the person (per D INS-04)."
    - "The trend card carries a consistency micro-insight caption instead of raw-count framing (per D INS-05)."
    - "All consent, privacy, safety, and demo-disclosure copy is byte-for-byte unchanged; widget_test assertions are migrated deliberately, never by loosening privacy assertions."
  artifacts:
    - "lib/screens/patient_dashboard.dart — rebuilt with-data composition (greeting block, score hero, _MiniMetricCard two-up, retitled trend card, consent last); PatientFirstRun empty-state gate untouched"
    - "test/widget_test.dart — migrated post-import assertions + new phone-width hierarchy regression test"
  key_links:
    - "patient_dashboard.dart -> computeSleepScore/insightLine/consistencyCaption/weekDeltaLabel in lib/services/sleep_insights.dart"
    - "patient_dashboard.dart -> ScoreRing/ContributorBar/InfoTip + nidToneColor in lib/screens/data_displays.dart"
    - "state.summaries.isEmpty collection-if still selects PatientFirstRun (Phase 10 layer) vs the new hierarchy"
---

<objective>
Rebuild the patient dashboard's with-data state into the design's Oura-grade hierarchy: greeting/status block with the one observational insight line, the multi-factor score ring hero with contributors and the "not a diagnosis" headnote, two calm secondary mini-cards, the honest trend card with a consistency micro-insight, and the consent card demoted to last — with every coupled widget_test assertion migrated in the same plan.

Purpose: INS-01 (hierarchy, hero readout, connection state out of the metric row), INS-02 (score ring shipped on the dashboard), INS-04 (the insight line), INS-05 patient side (micro-insight captions).
Output: Rebuilt `patient_dashboard.dart` with-data branch + green, deliberately-updated `widget_test.dart` including a new phone-width hierarchy test.
</objective>

## Phase Goal

**As a** patient (and their linked clinician), **I want to** see honest, hierarchical sleep insights — a real score, truthful charts, and gentle observations — **so that** the data feels trustworthy instead of raw and redundant. (Derived 1:1 from the ROADMAP Phase 11 goal; no scope added.)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-insightful-data-displays/11-CONTEXT.md
@.planning/phases/11-insightful-data-displays/11-DESIGN-SPEC.md
@.planning/phases/11-insightful-data-displays/11-01-SUMMARY.md
@lib/screens/patient_dashboard.dart
@lib/screens/patient_first_run.dart
@lib/services/sleep_insights.dart
@lib/screens/data_displays.dart
@lib/screens/common_widgets.dart
@lib/theme/app_theme.dart
</context>

## Artifacts this phase produces

- Plan 11-01: `NidStateColors` + `NidColors.faint` + sleep-hours ramp in `lib/theme/app_theme.dart`; `lib/services/sleep_insights.dart` (`StateTone`, `ScoreContributor`, `SleepScore`, `ClinicianWeekSummary`, `computeSleepScore`, `insightLine`, `consistencyCaption`, `weekDeltaLabel`, `clinicianWeekSummary`); `lib/screens/data_displays.dart` (`ScoreRing`, `ContributorBar`, `InfoTip`, `StatDeltaRow`, `nidToneColor`, `dayLetter`); honest `SleepTrendBars`; `test/services/sleep_insights_test.dart`; `test/data_displays_test.dart`.
- Plan 11-02 (this plan): rebuilt with-data `patient_dashboard.dart` — `_GreetingBlock`, `_ScoreHeroCard`, `_MiniMetricCard` (LAST NIGHT / 7-NIGHT AVERAGE), retitled 'Sleep trend · 7 nights' card with consistency caption, consent card last; copy constants 'SLEEP SCORE', 'not a diagnosis' (headnote), status headlines ('A protective night' / 'A steady night' / 'A lighter night' / 'A short night'); InfoTip copy for Sleep score and Consistency; updated `test/widget_test.dart` + new 'dashboard hierarchy' phone-width test.
- Plan 11-03: `clinician_dashboard.dart` directional stat-delta summary replacing raw count tiles; new directional-summary test in `test/clinician_affordance_test.dart`.

<tasks>

<task type="auto">
  <name>Task 1: Rebuild the with-data dashboard hierarchy (greeting -> hero -> mini-cards -> trend -> consent)</name>
  <read_first>.planning/phases/11-insightful-data-displays/11-DESIGN-SPEC.md sections "18-dashboard-mobile", "Dashboard target composition", "Component specs", "Copy bank" (read fully — this is the visual ground truth); lib/screens/patient_dashboard.dart (entire file — build 16-133, the metric Wrap 45-69, trend card 71-126, consent card 129 and _ConsentLifecycleCard 183-347 with its verbatim copy at 244-246 and 339-341, _MetricTile 411-445); lib/screens/patient_first_run.dart (the Phase 10 empty-state layer — must stay untouched); lib/services/sleep_insights.dart (API from plan 11-01); lib/screens/data_displays.dart (widget kit from plan 11-01); lib/state/app_state.dart lines 46-61 (getters: summaries, currentUser, healthPermissionStatus, isBusy)</read_first>
  <files>lib/screens/patient_dashboard.dart</files>
  <action>Rebuild ONLY the with-data branch of `PatientDashboard.build` (the `else ...[]` arm of the `state.summaries.isEmpty` collection-if; per D INS-01 the empty branch, `PatientFirstRun`, the gate itself, and `_ConsentLifecycleCard` and all of its copy stay byte-for-byte as-is). Compute once at the top of the branch: `final score = computeSleepScore(state.summaries);`.

New with-data order (per D INS-01, top to bottom):
1. `BrandHeader` stays first but drop its `trailing:` StatusPill entirely (the sharing-status pill at lines 26-36) — no sharing affordance in the Today hierarchy; the consent card at the bottom carries sharing state (per D INS-01). This header change applies to both branches (it sits above the gate) — that is intended.
2. `_GreetingBlock` (new private widget, NOT a card — plain centered Column, per D INS-01): (a) hello line 'Good {daypart}, {firstName}' where daypart = 'morning' when DateTime.now().hour < 12, 'afternoon' when < 17, else 'evening', and firstName = first whitespace-token of `state.currentUser.displayName` — style bodySmall.copyWith(fontWeight: FontWeight.w600); (b) status headline row: a 9px circular moss dot (NidColors.moss), gap NidSpace.s, then the headline text styled `headlineMedium.copyWith(fontSize: 30, color: NidColors.canopy, height: 1.12, letterSpacing: -0.6)` — headline mapped from score.tone: optimal -> 'A protective night', good -> 'A steady night', fair -> 'A lighter night', attention -> 'A short night' (observational, describes the night, never the person); (c) sub-line = `insightLine(state.summaries)` styled bodySmall, centered, maxWidth ~300 — this is THE one insight line on the dashboard (per D INS-04; do not add a second insight line elsewhere).
3. `_ScoreHeroCard` (new private widget wrapping SectionCard, per D INS-02): header Row of Text 'SLEEP SCORE' (labelSmall.copyWith(fontSize: 11, fontWeight: FontWeight.w700, letterSpacing: 1.0, color: NidColors.canopy)), then `InfoTip(term: 'Sleep score', body: 'A 0-100 summary of your recent sleep, combining duration, rhythm, and the week\'s direction. It compares only to your own recent nights, so one rough night barely moves it.')`, Spacer, and the headnote Text 'not a diagnosis' — VERBATIM lowercase, styled fontSize 11, w600, NidColors.faint. Below: Row of `ScoreRing(score: score.value, word: score.word, color: nidToneColor(score.tone))` (default 120 size) then gap NidSpace.l then Expanded Column of the three ContributorBars built from score.contributors (name, word, tone, fraction) — attach `InfoTip(term: 'Consistency', body: 'How similar your recent nights have been. Rhythm drifts during travel or busy weeks and settles again on its own.')` to the Consistency row via the infoTip slot. Below the row: caption Text(score.caption) styled fontSize 14, w600, color nidToneColor(score.tone). At narrow constraints (< 340px inner width via LayoutBuilder) stack ring above contributors instead of Row to avoid overflow.
4. Two-up secondary mini-cards (per D INS-01): Row of two Expanded `_MiniMetricCard`s (new private widget replacing the old `_MetricTile` — delete `_MetricTile`, it becomes unused): uppercase label (labelSmall canopy), value (displaySmall), caption (bodySmall). Card A label 'LAST NIGHT', value `hoursLabel(latest.sleepDurationHours)`, caption `latest.trendFlag`. Card B label '7-NIGHT AVERAGE', value = hoursLabel of the mean of the most recent up-to-7 summaries, caption `weekDeltaLabel(state.summaries)`.
5. The trend SectionCard stays (Import button, permission StatusPill + message, SleepTrendBars — all logic unchanged so the 'sleep access ready' pill keeps rendering post-import) with two additions: retitle 'Sleep trend' to 'Sleep trend · 7 nights', and add a micro-insight caption under SleepTrendBars — Text(`consistencyCaption(state.summaries)`, style bodySmall) (per D INS-05 patient side; replaces raw-count framing with a balance observation).
6. `SectionCard(child: _ConsentLifecycleCard(state: state))` remains LAST, after all metric content, untouched (per D INS-01 — demoted placement; Phase 12 builds the real sharing surface).

Remove the entire old metric Wrap (lines 45-69) including the 0-100 proxy tile (lines 56-60) and the connection-state tile (lines 61-67) — the score hero replaces the former; the latter leaves the metric hierarchy entirely (per D INS-01, D INS-02). The `DailySummary.sleepQualityProxy` model field and its persistence stay untouched (data plumbing is out of scope; only the display is rebuilt). Do not echo the removed tile labels anywhere, including comments. New copy rules: no emoji, no exclamation marks, Inter weights w400-w700 only, all colors via NidColors/NidStateColors/nidToneColor.</action>
  <verify>
    <automated>flutter analyze lib/screens/patient_dashboard.dart && ! grep -qi "quality proxy" lib/screens/patient_dashboard.dart && ! grep -qi "clinician link" lib/screens/patient_dashboard.dart && grep -q "not a diagnosis" lib/screens/patient_dashboard.dart && grep -q "SLEEP SCORE" lib/screens/patient_dashboard.dart && grep -q "insightLine" lib/screens/patient_dashboard.dart</automated>
  </verify>
  <acceptance_criteria>
    - With data, children render in order: BrandHeader (no trailing pill) -> _GreetingBlock -> _ScoreHeroCard -> mini-card Row -> trend SectionCard -> consent SectionCard last.
    - patient_dashboard.dart no longer contains the strings 'quality proxy' or 'clinician link' (case-insensitive); 'not a diagnosis' appears exactly once as the hero headnote.
    - The empty branch still renders PatientFirstRun via the untouched state.summaries.isEmpty gate; patient_first_run.dart has zero diff.
    - _ConsentLifecycleCard, its consent copy ('Only sleep summaries and samples become visible after consent...', 'Visible after acceptance: sleep samples, daily summaries, trend flags. Hidden: journal entries, drafts, private reflections.'), and consent history rendering are byte-for-byte unchanged.
    - Exactly one insight line renders (the greeting sub-line via insightLine); the trend caption uses consistencyCaption (a balance observation, not a second last-night comparison).
    - InfoTips present on the score label and the Consistency contributor with the exact copy given above.
  </acceptance_criteria>
  <done>The dashboard leads with one hero readout in the design's hierarchy, the rebuilt non-diagnostic score ring ships with contributors and humility copy, and the connection state no longer poses as a health metric.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate coupled widget_test assertions + add the phone-width hierarchy regression test</name>
  <read_first>test/widget_test.dart lines 14-46 (first test — post-import assertions to extend), 48-106 (reset test — dialog copy at line 91 must stay asserted verbatim), 272-305 (clinician privacy copy test — untouched), 329-334 and 418-459 (responsive surfaces test — empty-state flow, should stay green); lib/screens/patient_dashboard.dart (as rebuilt by Task 1); lib/screens/data_displays.dart (ScoreRing type for byType finds)</read_first>
  <files>test/widget_test.dart</files>
  <action>Update `test/widget_test.dart` deliberately — extend coverage for the new hierarchy WITHOUT touching any privacy, consent, safety, or demo-disclosure assertion (the reset-dialog copy at line 91, the 'Visible: sleep samples, daily summaries, trend flags.' scroll assertions, 'Accepted invites only. Sleep summaries, never journals.', and the demo-mode notice all stay exactly as written):

1. First test ('patient can sign up and import mock sleep data') — after the existing post-import assertions (`state.summaries` not empty, 'sleep access ready' findsOneWidget, first-run hero findsNothing), add: `expect(find.text('not a diagnosis'), findsOneWidget);`, `expect(find.text('SLEEP SCORE'), findsOneWidget);`, `expect(find.byType(ScoreRing), findsOneWidget);` (import `package:nguyenindoubt_app/screens/data_displays.dart`), `expect(find.textContaining('Good '), findsOneWidget);` (daypart-agnostic greeting), and negative checks that the old tiles are gone: `expect(find.text('QUALITY PROXY'), findsNothing);` and `expect(find.text('CLINICIAN LINK'), findsNothing);`.

2. New testWidgets 'dashboard hierarchy shows score hero, honest trend, and one insight line at phone width': set `tester.view.physicalSize = const Size(390, 844)` with devicePixelRatio 1 and the standard teardowns; build NguyenInDoubtState with InMemoryAppRepository + MockHealthDataProvider; pump `NguyenInDoubtApp(state: state, showSplash: false)` (the Phase 10 seam — every app-pumping test passes showSplash: false); complete onboarding via the existing `_completePatientOnboarding` helper; then `await state.importMockSleep(); await tester.pumpAndSettle();`. Assert: `tester.takeException()` is null (no overflow at phone width); ScoreRing findsOneWidget; 'not a diagnosis' findsOneWidget; the insight line renders — `find.textContaining('vs your recent average')` findsOneWidget (deterministic: the fixed mock durations put last night ~+46m over baseline); the honest-trend legend '5h short' and '8h+ optimal' each findsOneWidget; '8h' hairline label present (`find.text('8h')` findsOneWidget); the consent card is still reachable — `await tester.scrollUntilVisible(find.text('Sleep sharing consent'), 200)` then findsOneWidget.

3. Verify no other test in the file references the removed metric tiles or the removed BrandHeader trailing pill labels (per the current file none do — 'shared sleep'/'private' pill and tile labels are unasserted); if the responsive surfaces test or session-restore tests fail from layout changes, fix by scrolling (`ensureVisible`/`scrollUntilVisible`) rather than weakening assertions.</action>
  <verify>
    <automated>flutter test test/widget_test.dart && flutter test</automated>
  </verify>
  <acceptance_criteria>
    - flutter test passes with the full suite green; no privacy/consent/safety/demo-disclosure assertion was modified or removed (git diff of test/widget_test.dart shows only additions and the documented first-test extensions).
    - The new phone-width test pumps with showSplash: false, imports data, and proves: no exception, ScoreRing + 'not a diagnosis' + 'SLEEP SCORE' present, exactly one 'vs your recent average' insight line, '5h short'/'8h+ optimal'/'8h' trend scale texts present, consent card last but reachable.
    - 'QUALITY PROXY' and 'CLINICIAN LINK' find nothing post-import.
  </acceptance_criteria>
  <done>The coupled suite proves the new hierarchy end-to-end at phone width and pins the honest-chart and non-diagnostic copy as regressions, with privacy assertions untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| on-device sleep data -> patient-visible insight copy | Greeting headline, score, contributors, and insight line derive only from the patient's own DailySummary list on a patient-only surface |
| Today surface -> sharing/consent affordances | Sharing status leaves the metric hierarchy; consent card (with verbatim disclosure copy) remains the single sharing surface until Phase 12 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-02-ID | Information Disclosure | rebuilt patient dashboard | low | mitigate | The dashboard is a patient-only surface; all new strings derive from sleep summaries via sleep_insights.dart (DailySummary-only signatures); consent/privacy copy is byte-for-byte preserved and test-asserted. |
| T-11-02-RP | Repudiation/misrepresentation | score hero card | medium | mitigate | Verbatim 'not a diagnosis' headnote rendered on the hero and pinned by a findsOneWidget test; state words and headlines describe the night/metric, never the person; no exclamation marks or diagnostic vocabulary. |
| T-11-02-TMP | Tampering | Import + permission flow | low | accept | The trend card keeps the existing importMockSleep wiring and permission pill logic unchanged; no new data path or capability is introduced. |
| T-11-SC | Tampering | package installs | low | mitigate | No installs; pubspec.yaml unmodified. |
</threat_model>

<verification>
Plan-level bar (run all, must pass):
- `dart format lib test` (0 changed)
- `flutter analyze` (no issues)
- `flutter test` (all green)
- `flutter build web`

Human-check (advisory, end-of-phase): after importing sleep, the dashboard reads greeting -> score ring hero ('not a diagnosis' visible, ring eases in) -> two mini-cards -> honest trend with 8h hairline and ramp legend -> consent card at the very bottom; nothing about sharing appears above the consent card.
</verification>

<success_criteria>
- The dashboard has one clear hero readout and no connection toggle posing as a health metric (INS-01, ROADMAP SC-1).
- The 0-100 measure is rebuilt as a real multi-factor non-diagnostic ring with an explicit scale (number + state word + contributors + 'not a diagnosis') (INS-02, ROADMAP SC-2).
- Exactly one gentle observational insight line is present and never labels the person (INS-04, ROADMAP SC-4).
- The trend card carries a consistency micro-insight caption (INS-05 patient side, ROADMAP SC-5).
</success_criteria>

<output>
Create `.planning/phases/11-insightful-data-displays/11-02-SUMMARY.md` when done.
</output>
